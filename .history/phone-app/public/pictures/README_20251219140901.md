# ⚠️ THÔNG BÁO QUAN TRỌNG

**Thư mục này KHÔNG còn lưu ảnh nữa!**

## 📸 Cấu trúc mới - Centralized Storage

Tất cả hình ảnh giờ được quản lý tập trung tại:

```
backend/public/pictures/  ← NGUỒN DUY NHẤT
```

## 🔄 Cách hoạt động

1. **Backend serve static files**: `http://localhost:4000/pictures/...`
2. **Phone-app fetch từ backend**: Sử dụng helper `buildImageUrl()`
3. **Không còn duplicate**: Tiết kiệm dung lượng và tránh không đồng bộ

## 💻 Sử dụng trong code

```tsx
import { buildImageUrl } from '@/lib/image'

// Cũ (❌):
<Image src="/pictures/iphone/iphone-15.png" />

// Mới (✅):
<Image src={buildImageUrl("/pictures/iphone/iphone-15.png")} />
// => "http://localhost:4000/pictures/iphone/iphone-15.png"
```

## 🆕 Upload ảnh mới

**Chỉ upload vào backend:**
1. Đặt file vào `backend/public/pictures/[category]/`
2. Hoặc dùng API upload: `POST /api/products/upload`
3. Ảnh tự động được serve qua `http://localhost:4000/pictures/`

## 🚀 Production

Trên production, update `NEXT_PUBLIC_API_BASE_URL` trong `.env.local`:
```
NEXT_PUBLIC_API_BASE_URL=https://api.yoursite.com/api
```

Helper sẽ tự động trỏ đến: `https://api.yoursite.com/pictures/...`
